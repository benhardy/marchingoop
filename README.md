March In Goop
=============


A raymarcher that runs in the GPU. Work in progress.

Build and run with Maven, e.g.:

`mvn compile exec:java -Dexec.mainClass=net.aethersanctum.marchingoop.main.GpuDemo`
